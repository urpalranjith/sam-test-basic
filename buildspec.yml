version: 0.2

phases:
    install:
        commands:
            - echo started...

    pre_build:
        commands:
            - echo building npm
            - npm install           

    build:
        commands:
            - echo deployment started..
            - aws cloudformation package --template-file template.yml --output-template-file package.yml --s3-bucket ranjith-sam-code
            
    post_build:
        commands:
            - echo deployment complete..
artifacts:
    files:
        - package.yml
